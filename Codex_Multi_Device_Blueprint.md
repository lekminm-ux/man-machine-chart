# Blueprint: การใช้งาน Codex หลายเครื่องกับ Project เดียวกัน

## เป้าหมาย

สร้างแนวทางใช้งาน Codex ระหว่างเครื่องหลายเครื่อง เช่น เครื่อง no.1 และเครื่อง no.2 โดยให้ทั้งสองเครื่องทำงานกับโฟลเดอร์โปรเจกต์เดียวกัน และสามารถทำงานต่อจากสถานะล่าสุดได้อย่างปลอดภัย แม้ประวัติแชทของ Codex จะไม่ได้ซิงค์ข้ามเครื่องครบถ้วนเสมอไป

---

## แนวคิดหลัก

การใช้งาน Codex หลายเครื่องต้องแยกให้ออกเป็น 2 ส่วน:

1. **ไฟล์โปรเจกต์**  
   เช่น source code, database schema, document, requirement, config file  
   ส่วนนี้สามารถซิงค์ข้ามเครื่องได้ผ่าน OneDrive, Git, Google Drive หรือโฟลเดอร์ร่วม

2. **ประวัติแชท / บริบทการคุยกับ Codex**  
   ส่วนนี้ไม่ควรคาดหวังว่าจะตามไปทุกเครื่องเสมอ โดยเฉพาะกรณีใช้ Codex ผ่าน local / CLI / IDE

ดังนั้น วิธีที่ปลอดภัยที่สุดคือให้โปรเจกต์มีไฟล์บันทึกบริบทและสถานะงานไว้ในโฟลเดอร์โปรเจกต์เอง

---

## โครงสร้างโฟลเดอร์ที่แนะนำ

```text
D:\OneDrive\MyProject\
│
├─ PROJECT_CONTEXT.md
├─ CHANGELOG_AI.md
├─ README.md
├─ docs\
├─ src\
├─ database\
└─ package.json
```

---

## ไฟล์ที่ควรมีในโปรเจกต์

### 1. PROJECT_CONTEXT.md

ใช้บอกภาพรวมของโปรเจกต์ให้ Codex / Claude / ChatGPT เข้าใจตรงกัน

ควรมีข้อมูล เช่น:

- ชื่อโปรเจกต์
- เป้าหมายของระบบ
- เทคโนโลยีที่ใช้
- โครงสร้างโฟลเดอร์
- กฎสำคัญของโปรเจกต์
- สิ่งที่ห้ามแก้โดยไม่ขออนุญาต
- ข้อมูลที่เกี่ยวข้องกับ Database, Power BI, Excel หรือ Workflow

ตัวอย่าง:

```md
# PROJECT_CONTEXT

## Project Name
MES Web App / Production Report System

## Purpose
ระบบนี้ใช้สำหรับบันทึกข้อมูลการผลิต, OEE, Manhour, Downtime, Defect และ Plan OT

## Current Tech
- Frontend: React / Next.js
- Backend: Node.js
- Database: SQL
- Data source เดิม: Excel / Google Sheets

## Important Rules
- ห้ามแก้โครงสร้าง Database โดยไม่อธิบายเหตุผล
- ทุกครั้งที่แก้ logic ต้องอัปเดต CHANGELOG_AI.md
- ต้องรักษาชื่อ field ที่ใช้กับ Power BI ให้สอดคล้องกัน
- ถ้าไม่แน่ใจ ให้เสนอแผนก่อนแก้จริง

## Important Folders
- /src = source code
- /database = schema และ migration
- /docs = เอกสาร requirement
```

---

### 2. CHANGELOG_AI.md

ใช้บันทึกสิ่งที่ AI เคยแก้ไขหรือวิเคราะห์ไว้แล้ว เพื่อให้เปิดจากเครื่องอื่นแล้วทำงานต่อได้

ควรมีข้อมูล เช่น:

- วันที่ทำงาน
- งานที่ทำเสร็จแล้ว
- ไฟล์ที่แก้ไข
- เหตุผลที่แก้
- ปัญหาที่ยังค้างอยู่
- ขั้นตอนถัดไป

ตัวอย่าง:

```md
# CHANGELOG_AI

## 2026-07-08

### Done
- เพิ่มตาราง Plan OT
- แก้ logic การคำนวณ Actual OT
- ปรับ schema ให้รองรับข้อมูลจาก Excel sheet Database

### Files Changed
- database/schema.sql
- src/modules/ot/ot.service.ts
- docs/requirement.md

### Next Step
- ตรวจสอบ mapping ระหว่าง Plan OT กับ Actual OT
- ทดสอบ export ข้อมูลไป Power BI

### Notes
- ยังไม่ได้สรุป field สุดท้ายของ Manhour
- ต้องระวัง column ชื่อ Employee ID กับ EmpCode อาจซ้ำความหมาย
```

---

## วิธีใช้งานกับเครื่อง no.1 และ no.2

### เครื่อง no.1: เครื่องหลัก

1. เปิดโปรเจกต์จากโฟลเดอร์หลัก เช่น OneDrive หรือ Git repository
2. ให้ Codex อ่านไฟล์บริบทก่อนเริ่มงาน
3. ทำงาน แก้ code หรือวิเคราะห์ระบบตามปกติ
4. หลังทำงานเสร็จ ให้ Codex อัปเดต `CHANGELOG_AI.md`
5. ถ้ามีการเปลี่ยน requirement หรือโครงสร้างระบบ ให้ Codex อัปเดต `PROJECT_CONTEXT.md`

Prompt ที่ใช้ได้:

```text
อ่าน PROJECT_CONTEXT.md และ CHANGELOG_AI.md ก่อน แล้วสรุปสถานะล่าสุดของโปรเจกต์ จากนั้นเสนอแผนก่อนเริ่มแก้ code
```

หลังทำงานเสร็จ:

```text
สรุปสิ่งที่แก้ไขทั้งหมด และอัปเดต CHANGELOG_AI.md ให้เรียบร้อย โดยระบุไฟล์ที่แก้ เหตุผล และ next step
```

---

### เครื่อง no.2: เครื่องที่ใช้นาน ๆ ครั้ง

1. เปิดโฟลเดอร์โปรเจกต์เดียวกันกับเครื่อง no.1
2. ตรวจสอบว่าไฟล์ซิงค์ล่าสุดแล้ว
3. ให้ Codex อ่าน `PROJECT_CONTEXT.md` และ `CHANGELOG_AI.md` ก่อนเสมอ
4. ห้ามให้ Codex เริ่มแก้ code ทันทีโดยไม่อ่าน context

Prompt ที่ใช้ได้:

```text
ก่อนเริ่มงาน ให้อ่าน PROJECT_CONTEXT.md และ CHANGELOG_AI.md ทั้งหมดก่อน จากนั้นสรุปว่าตอนนี้โปรเจกต์อยู่สถานะไหน และบอกว่าควรทำอะไรต่อ
```

ถ้าจะให้แก้ไขต่อ:

```text
ทำงานต่อจากสถานะล่าสุดใน CHANGELOG_AI.md ก่อนแก้ code ให้เสนอแผนและไฟล์ที่จะกระทบก่อน
```

---

## คำตอบสำคัญเรื่อง Chat History

ถ้าใช้ Codex หลายเครื่อง:

- ไฟล์โปรเจกต์สามารถตามไปเครื่องอื่นได้ ถ้าใช้โฟลเดอร์เดียวกันหรือ sync ผ่าน OneDrive/Git
- Chat history หรือ session เดิม อาจไม่ได้ตามไปครบทุกเครื่อง
- ไม่ควรใช้ chat history เป็นแหล่งความจริงหลักของโปรเจกต์
- ควรใช้ `PROJECT_CONTEXT.md` และ `CHANGELOG_AI.md` เป็นแหล่งความจริงหลักแทน

---

## Workflow ที่แนะนำสำหรับงานจริง

```text
1. เปิดโปรเจกต์
2. ให้ AI อ่าน PROJECT_CONTEXT.md
3. ให้ AI อ่าน CHANGELOG_AI.md
4. ให้ AI สรุปสถานะล่าสุด
5. ให้ AI เสนอแผนก่อนแก้
6. อนุมัติแผน
7. ให้ AI แก้ code / docs / database
8. ทดสอบ
9. ให้ AI อัปเดต CHANGELOG_AI.md
10. Commit เข้า Git หรือรอ OneDrive sync ให้เสร็จ
```

---

## Prompt มาตรฐานที่ควรใช้ทุกครั้ง

### Prompt เริ่มงาน

```text
อ่าน PROJECT_CONTEXT.md และ CHANGELOG_AI.md ก่อนทุกครั้ง
จากนั้นสรุปสถานะล่าสุดของโปรเจกต์
ห้ามแก้ code จนกว่าจะเสนอแผนและได้รับอนุมัติ
```

### Prompt ก่อนแก้ Code

```text
ก่อนแก้ไข ให้ระบุ:
1. ไฟล์ที่จะถูกแก้
2. เหตุผลที่ต้องแก้
3. ความเสี่ยงที่อาจกระทบระบบเดิม
4. วิธีทดสอบหลังแก้
```

### Prompt หลังแก้ Code

```text
หลังแก้เสร็จ ให้สรุป:
1. แก้อะไรไปบ้าง
2. แก้ไฟล์ไหน
3. ทดสอบอะไรแล้ว
4. ยังมีอะไรค้างอยู่
5. อัปเดต CHANGELOG_AI.md ให้เรียบร้อย
```

---

## ข้อควรระวัง

### OneDrive

ใช้ OneDrive ได้สำหรับซิงค์ไฟล์ แต่ควรระวัง:

- ไฟล์อาจยังซิงค์ไม่เสร็จ
- อาจเกิด conflict ถ้าเปิดแก้พร้อมกันหลายเครื่อง
- ไม่เหมาะสำหรับ rollback version แบบจริงจัง

### Git

ถ้าเป็นโปรเจกต์ code จริงจัง ควรใช้ Git ร่วมด้วย เพราะ:

- ย้อน version ได้
- เห็นประวัติการแก้ไขชัดเจน
- แยก branch ได้
- ป้องกันการแก้ทับกันได้ดีกว่า OneDrive

แนวทางที่แนะนำ:

```text
OneDrive = ใช้ซิงค์ไฟล์ระหว่างเครื่อง
Git = ใช้ควบคุม version ของ code
PROJECT_CONTEXT.md + CHANGELOG_AI.md = ใช้ควบคุมบริบทให้ AI
```

---

## สรุป

ไฟล์ `PROJECT_CONTEXT.md` และ `CHANGELOG_AI.md` ไม่ใช่ไฟล์ที่ Codex สร้างให้อัตโนมัติ แต่เป็นไฟล์ที่ผู้ใช้สร้างเองในโฟลเดอร์โปรเจกต์ เพื่อใช้เป็นความจำกลางของโปรเจกต์

เมื่อใช้หลายเครื่อง เช่น no.1 และ no.2 ให้ถือว่า:

- ไฟล์โปรเจกต์คือสิ่งที่ซิงค์ข้ามเครื่อง
- chat history อาจไม่ซิงค์ครบ
- context สำคัญต้องถูกเขียนลงไฟล์ในโปรเจกต์
- ทุกครั้งที่ย้ายเครื่อง ให้สั่ง Codex อ่านไฟล์ context ก่อนเริ่มงาน

