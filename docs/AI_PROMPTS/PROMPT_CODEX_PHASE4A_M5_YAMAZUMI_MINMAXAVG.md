# Prompt — Codex Implementation: Phase 4a, M5 Yamazumi Min/Max/Avg Overlay

Paste this whole block into Codex / GPT-5.6 Luna Max. This is an
IMPLEMENT_ONLY handoff written by Claude after reading the real source files
and confirming the plan with the user — do not redesign the approach, and do
not expand scope beyond what's listed below. When done, report back to
**Claude** (not the user directly) using the REQUIRED HANDOFF OUTPUT format
at the end, so Claude can review before anything is committed, pushed, or
deployed.

```text
ROLE: Codex / GPT-5.6 Luna Max — implementation only
MODE: IMPLEMENT_ONLY — build exactly the plan below; do not redesign, do not
expand scope, do not start Phase 4b or 4c

PROJECT ROOT
D:/00_LocalFile_WebApp/ManMachineChart_StandardOperation

CONTEXT
Phase 0C (Save-to-Cloud Persistence) is complete, released, and deployed to
Production. The project is now returning to the main feature roadmap. Phase 4
is "M5 Yamazumi — complete version," with three independent sub-features of
very different risk: (4a) Min/Max/Average overlay on the existing bars — this
prompt; (4b) a new Periodical-vs-Changeover classification field — separately
scoped later, needs its own authorization since it adds a new persisted
field; (4c) Drag & Drop rebalancing — separately scoped later, UI-only. Do
NOT implement 4b or 4c in this round.

ROOT CAUSE (read this before touching code)
`Module5_YamazumiChart.tsx` currently builds each operator's bar from
`activeFile.steps` (Module 4) via `getCalculatedSteps()`, where each step
holds exactly one duration value per field — there is no Min/Max/Average
variability to show, because that concept only exists in Module 1's
`TimeStudy.rows[].readings[]` (multiple stopwatch reps per job element).

The calculation Module 5 needs already exists and is already proven correct:
`computeOperatorTotals(study: TimeStudy)` in `src/lib/time-study.ts:113`
aggregates Min/Max/Average per operator from Module 1 data, and is already
consumed today by `Module1_TimeMeasurement.tsx:49` (the per-worker summary
cards at the bottom of Module 1). It is NOT yet called from Module 5 — that
is the entire gap. `computeOperatorTotals` currently returns `min`/`max`/
`average` combined across all row kinds (man+walk+idle) for one operator; it
does NOT yet split `min` by kind, which Module 5 needs to keep its existing
Manual/Walk/Idle color-coded stacking (the user explicitly chose to keep
this rather than switch to a single generic color).

REQUIRED SCOPE — ALLOWED TO CHANGE, exactly these files, nothing else
- src/lib/time-study.ts
- src/components/modules/Module5_YamazumiChart.tsx
- tests/time-study.test.cjs
- CHANGELOG_AI.md

READ FIRST — exact paths, in this order
1. D:/00_LocalFile_WebApp/ManMachineChart_StandardOperation/PROJECT_CONTEXT.md
2. D:/00_LocalFile_WebApp/ManMachineChart_StandardOperation/CHANGELOG_AI.md (latest entries)
3. D:/00_LocalFile_WebApp/ManMachineChart_StandardOperation/docs/Master_Plan.html
   — read section id="status" (module status table) and id="roadmap" (Phase 4
   description) for the M5 spec: "แท่งฐาน = Min จาก M1 · ส่วนทับด้านบน = Max ·
   จุดกลางแท่ง = Average" and "เส้นแดงแนวนอน = Takt Time Benchmark"
4. D:/00_LocalFile_WebApp/ManMachineChart_StandardOperation/src/lib/time-study.ts
   (full file — read `computeRowStats`, `computeOperatorTotals`, and how
   `isMachineRow` groups rows)
5. D:/00_LocalFile_WebApp/ManMachineChart_StandardOperation/src/components/modules/Module5_YamazumiChart.tsx
   (full file — this is what you're changing)
6. D:/00_LocalFile_WebApp/ManMachineChart_StandardOperation/src/components/modules/Module1_TimeMeasurement.tsx
   — READ-ONLY reference. This is the cross-module number-check baseline
   (line ~399-414 shows how `computeOperatorTotals` output is already
   rendered as Min/Max/Aver cards). Do not edit this file.
7. D:/00_LocalFile_WebApp/ManMachineChart_StandardOperation/src/lib/chart-utils.ts
   (`getCalculatedSteps`, used for the fallback path)
8. D:/00_LocalFile_WebApp/ManMachineChart_StandardOperation/src/types/index.ts
   (`ChartFile.timeStudy` is optional — confirm you understand why the
   fallback path below is required)
9. D:/00_LocalFile_WebApp/ManMachineChart_StandardOperation/tests/time-study.test.cjs
   (existing test patterns to match)

PREFLIGHT
- Run: git status --short --branch
- Preserve all existing uncommitted/untracked files exactly as found. Do not
  reset, checkout, restore, delete, or run any broad cleanup.
- If anything unexpected is already in the working tree (files you don't
  recognize, a diff you didn't make), stop and report before touching
  anything.

FORBIDDEN
- Do not edit any file outside the 4 listed in REQUIRED SCOPE.
- Do not touch `Module1_TimeMeasurement.tsx`, `useChartStore.ts`,
  `types/index.ts`, `schema.sql`, `functions/api/*`, or any test file other
  than `tests/time-study.test.cjs`.
- Do not implement the Periodical/Changeover field (Phase 4b) or Drag & Drop
  (Phase 4c) — not even a partial/stub version.
- Do not change the existing fields on `OperatorTotal`
  (`min`/`max`/`average`/`rowCount`) or their values — only add new fields.
- Do not commit, push, deploy, or run any git-mutating command.
- Do not write to Production D1, use `--remote`, run a migration, reset, or
  seed anything.
- If implementing this plan turns out to require touching a file outside
  scope, or changing an existing field's meaning, STOP and report
  `PLAN_CHANGE_REQUIRED` with the specific reason — do not improvise around
  it.

IMPLEMENTATION PLAN

1. In `src/lib/time-study.ts`, extend the `OperatorTotal` interface and
   `computeOperatorTotals()` to additionally return `manMin`, `walkMin`,
   `idleMin`: the sum of `computeRowStats(row).min` for that operator's rows,
   split by `row.kind` (`'man'` / `'walk'` / `'idle'`; machine-kind rows stay
   grouped under the existing `'Auto M/C'` operator bucket exactly as today).
   `manMin + walkMin + idleMin` must equal the existing `min` field for that
   operator — add this as an explicit invariant your test checks, not just an
   assumption.

2. In `Module5_YamazumiChart.tsx`, change the per-operator bar construction:
   - If `activeFile.timeStudy?.rows?.length` is greater than 0: call
     `computeOperatorTotals(activeFile.timeStudy)` and, for each active
     operator, render:
     - a base stack of up to 3 segments (Manual/Walk/Idle, same colors as
       today: slate-800 / emerald-500 / red-500) sized by `manMin` / `walkMin`
       / `idleMin`, stacked to total height `totalMin` (the existing `min`
       field)
     - one overlay segment above that, from `totalMin` to `totalMax` (the
       existing `max` field), styled distinctly from the 3 solid colors
       (e.g. a lighter/translucent fill or diagonal hatch — your choice, but
       it must be visually unambiguous that it is NOT confirmed base work)
     - one marker (a short horizontal line, tick, or dot — your choice) at
       height `totalAverage` (the existing `average` field), visually
       distinct from both the solid stack and the overlay
   - If `activeFile.timeStudy` is missing or has zero rows: keep the CURRENT
     behavior byte-for-byte (bar built from `getCalculatedSteps(activeFile.steps)`,
     no overlay, no marker). This is a hard requirement — charts that have
     never used Module 1 must render exactly as they do today, no error, no
     blank chart.
   - Recompute the Y-axis scale (`maxTotalTime`/`Y_MAX`) so the tallest value
     across all operators — `totalMax` when using the M1 path, or the
     existing fallback total otherwise — plus Takt Time all fit without
     clipping.
   - Update the legend to explain the new overlay (label it "Max") and
     marker (label it "Average") alongside the existing Manual/Walking/Idle
     entries.

3. In `tests/time-study.test.cjs`, add tests proving:
   - `manMin`/`walkMin`/`idleMin` are correct for an operator with mixed
     man/walk/idle rows, and isolated correctly from a second operator's rows
     (grouping doesn't leak across operators).
   - `manMin + walkMin + idleMin === min` for every operator in the fixture.
   - The existing `min`/`max`/`average`/`rowCount` values for an existing
     test fixture (reuse one already in the file if suitable) are unchanged
     from before this change — a literal regression check.

ACCEPTANCE CRITERIA
1. `computeOperatorTotals()` returns correct `manMin`/`walkMin`/`idleMin`,
   proven by the new tests, with the sum invariant holding.
2. `Module1_TimeMeasurement.tsx`'s existing behavior/output is completely
   unaffected (you did not edit it, and the fields it reads are unchanged).
3. Opening a chart with populated `timeStudy` data across multiple operators
   shows, per operator: the 3-color base stack to `totalMin`, an overlay to
   `totalMax`, and an average marker — and these three numbers match exactly
   what Module 1's own per-worker summary cards show for the same operator.
4. Opening a chart with no `timeStudy` data renders identically to the
   current (pre-change) behavior — no crash, no blank chart, no console
   error.
5. `node --test`, `npm run lint`, `npm run build`, `git diff --check` all
   pass. Lint must show only the known baseline 5 errors (StepTable.tsx x3,
   Sidebar.tsx x1, TopBar.tsx x1) — zero new errors in any file you touched.

VERIFICATION — run exactly these, report exact output
- git status --short --branch
- node --test
- npm run lint
- npm run build
- git diff --check
- Start the dev server, open a chart with Module 1 data entered for at least
  2 different operators with varying readings (or create one), switch to
  Module 5, visually confirm the 3-part bar (base/overlay/marker) renders
  correctly and matches Module 1's numbers, check the browser console is
  clean, then open a chart that has never used Module 1 and confirm Module 5
  still renders exactly as before with no error.

SESSION RECORDS
Append a new entry to CHANGELOG_AI.md (do not edit older entries) recording:
files changed, the exact new fields added to `OperatorTotal`, the fallback
behavior you preserved, and the verification results above.

REQUIRED HANDOFF OUTPUT — report this back to Claude, not the user
- STATUS: IMPLEMENTED / TESTS_FAILED / PLAN_CHANGE_REQUIRED / BLOCKED
- Exact files read
- Exact files changed (must match the 4 allowed files exactly)
- The 3 new field names added to `OperatorTotal` and confirmation the 4
  existing fields are byte-identical in behavior
- Exact test/lint/build/diff-check output
- Manual browser verification result (both the with-M1-data case and the
  no-M1-data fallback case), including console result
- Any scope question or ambiguity you hit and how you resolved it (or, if
  you couldn't resolve it safely, why you stopped instead)
- Explicit statement: no commit, push, deploy, or Production D1 write
  occurred
- Next action: return this handoff to Claude for review. Do not proceed to
  Phase 4b, 4c, or any release action yourself.
```
